---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"a">;

const { href, class: className, ...props } = Astro.props;

const { pathname } = Astro.url;
const isActive = typeof href === "string" ? (href === "/" ? pathname === href : pathname.startsWith(href)) : false;
---

<a
  href={href}
  class:list={[
    "shadow-sm font-medium text-sm w-max h-full px-4 py-2",
    className,
    { "bg-black text-white dark:bg-white dark:text-black border-0 shadow-none": isActive },
  ]}
  {...props}
>
  <slot />
</a>
